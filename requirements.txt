# FastAPI 核心框架
fastapi==0.103.1
uvicorn==0.23.2  # 异步服务器

# 网络请求与数据处理
requests==2.31.0
python-multipart>=0.0.6  # 支持表单数据

# 配置与环境变量
pydantic-settings>=2.0.0
python-dotenv==1.0.0

# 身份认证
pyjwt>=2.8.0
python-jose[cryptography]>=3.3.0

# 数据库
pymysql==1.1.0
sqlalchemy>=2.0.0

# 录音功能依赖（新增）
sounddevice==0.4.6  # 麦克风音频采集
soundfile==0.12.1    # 音频文件读写（支持MP3/WAV）
numpy==1.26.0        # 音频数据合并与处理（解决数组拼接）